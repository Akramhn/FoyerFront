import { BlocHoverDirective } from './bloc-hover.directive';

describe('BlocHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new BlocHoverDirective();
    expect(directive).toBeTruthy();
  });
});
