import { HOVERTableDirective } from './hovertable.directive';

describe('HOVERTableDirective', () => {
  it('should create an instance', () => {
    const directive = new HOVERTableDirective();
    expect(directive).toBeTruthy();
  });
});
